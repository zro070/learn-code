# 深入浅出程序设计竞赛基础篇-个人笔记

## C1 语言入门

- 浮点数计算 整除 ->浮点数 tips: .0  /  double
- 科学计数法
- 函数原型-自动类型转换（隐式）
- **正难则反**
- 数据类型-取值范围
- ASCII 字符-数字  `char ans ='A';` `ans += 1;` 变为字符B
- 四舍五入 tips:  int(ans +0.5)  强制类型转换（显式）-小括号

- 例 2-7 （P5705）数字反转 ：分离数位 
- scanf printf  %占位符
- 关闭同步流加快cin读入速度
- swap （可加减）： `a=a-b; b=a+b; a=b-a;`
- 进位判断——整除和取余 
- 例 2-11（P3954）：浮点数误差  使用整除避免浮点数 /10
- 例 2-12（P5707）：基准点
- ceil 向上取整 取第一个>x 的整数
- 关系表达式：结果不是0就是1，有关系运算符组成 `> < >= <=`
- 运算符优先级  算术>关系
- 浮点数相等判断：不用`==`，而是看差值是否小于某个很小的数
- ^ 异或 两个条件刚好有一个成立 ——相同为0，不同为1
- 分支 `switch(变量){case 常量（整数/字符）: 执行语句; break;}`
- 浮点数-多重if嵌套判断
- `if   else if` ——else
- 例 3-10（P1085）：变量 初始化，维护和更新
- 例 3-11（P1909）：比较——打擂台法  判断+赋值
- 问号表达式 `x>y ? a : b`  问号前表达式成立，返回a，否则返回b
- 字符与整数的转换 —— `int z = c - '0';`